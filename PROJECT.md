# Internal findings

- 2026-09-09 | Switching shortcuts: one session CGEvent tap owns key consumption; avoid competing NSEvent runtime monitors. Match key-down modifiers directly, preserve queued action order, cancel queued actions on settings/recording changes. Recording suspends switching until save/Escape/settings focus loss. `Sources/DockAppToggler/Features/Shortcuts/{KeyboardShortcutMonitor,SwitchingShortcutState}.swift`; `Sources/DockAppToggler/UI/StatusBar/StatusBarPopoverAndSettings.swift`.
- 2026-09-09 | `build.sh`: verify signed bundle before installation; stage on destination volume + Foundation whole-bundle replacement, never recursive merge into installed app. Regression command: `python3 Tests/BuildScriptTests/test_installation.py`. Incident: Ghostty `com.mitchellh.ghostty` denied `SystemPolicyAppBundles`; failed `sudo cp -R` left installed Sparkle invalid.
- 2026-09-09 | App restart: settings/memory monitor/permission wizard share `NSApplication.restart`; LaunchServices new instance + `NSApp.terminate`, never `execv` (same PID, skipped AppKit shutdown/menu-bar cleanup). `Sources/DockAppToggler/Core/Extensions/NSApplication+Extensions.swift`. Integration: `python3 Tests/RestartTests/test_restart.py`; real AppKit processes, repeated restarts/double clicks, termination notifications + PID exit, `--s` on/off.
- 2026-09-09 | Startup permissions: `main.swift` → `AppPermissionRequester.checkStartupPermissions`; AX → ListenEvent → ScreenCapture; defer DockWatcher/StatusBarWatcher/KeyboardShortcutMonitor/help until verified. `PermissionWizardProgress` defers restart until every step checked/confirmed; manual confirmation advances setup only, never persists authorization. Every launch checks all three afresh; feature requests preserve active startup sequence. Regression: `Tests/DockAppTogglerTests/PermissionWizardTests.swift`.
